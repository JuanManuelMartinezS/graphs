export const startSimulation = (routeName) => {
    // Lógica de simulación a implementar
    console.log(`Simulación iniciada para la ruta: ${routeName}`);
    return `La simulación para la ruta "${routeName}" aún no está implementada.`;
  };